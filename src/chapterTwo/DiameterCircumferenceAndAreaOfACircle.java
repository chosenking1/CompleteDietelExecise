package chapterTwo;

public class DiameterCircumferenceAndAreaOfACircle {

    public static String calculate(int radius) {
        return (2*radius + " " + 2 * Math.PI * radius + " " + Math.PI * radius * radius);
    }
}
