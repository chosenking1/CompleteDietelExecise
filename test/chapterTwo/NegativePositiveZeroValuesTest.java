package chapterTwo;
//2.32
//        (Negative, Positive and Zero Values)
//        Write a program that inputs five numbers and
//        determines and prints the number of negative numbers input, the number of positive numbers input and
//        the number of zeros input.
public class NegativePositiveZeroValuesTest {
//    String result = NegativePositiveZeroValues.check(1);
//    String result = NegativePositiveZeroValues.check(2);
//    String result = NegativePositiveZeroValues.check(-3);
//    String result = NegativePositiveZeroValues.check(0);
//    String result = NegativePositiveZeroValues.check(6);
//    assertEquals(,result);
}
